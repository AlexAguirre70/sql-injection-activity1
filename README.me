##Able to bypass the login by entering this injection in either the username input

#' or 1=1 --

This searches for the username = '' and then the -- comments out the rest of the query

Using the correct UserName and injection the same code bypasses the password validation
#' or 1=1
